<%= form_with(model: responsable, local: true) do |form| %>
  <% if responsable.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(responsable.errors.count, "error") %> prohibited this responsable from being saved:</h2>

      <ul>
      <% responsable.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :observation %>
    <%= form.text_field :observation %>
  </div>

  <div class="field">
    <%= form.label :date %>
    <%= form.date_select :date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
